FOUR separate csvs --> hashPrime.csv, hashSeed.csv, bloomPrime.csv, bloomSeed.csv 

Testing Hashes:
    per line:
        random value, linear value, primeRand, primeLinear, timeRand, timeLinear

    axes of dist of bins: y is the bin (hashed index) , x is the passed through value

Testing Blooms:
    Per line:
        c, k, FPRRand, FPRLinear

